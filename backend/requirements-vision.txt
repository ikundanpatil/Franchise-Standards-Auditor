# Optional — local YOLO vision service (app/integrations/vision_service.py).
# Heavy (pulls in torch); install only where VISION_BACKEND=yolo is used.
#   pip install -r requirements.txt -r requirements-vision.txt
-r requirements.txt

ultralytics==8.3.40
pillow==11.0.0

# ---------------------------------------------------------------------------
# FranchiseGuard AI — backend runtime dependencies
# Python 3.12.  Pin exact versions so builds are reproducible.
# ---------------------------------------------------------------------------

# --- Web framework -------------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20          # OAuth2PasswordRequestForm / file uploads
orjson==3.10.12                   # fast JSON responses

# --- Data / validation -------------------------------------------------------
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0            # pydantic EmailStr

# --- Persistence -----------------------------------------------------------
SQLAlchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10           # PostgreSQL / Supabase driver

# --- Auth / security -------------------------------------------------------
PyJWT==2.10.1
passlib[bcrypt]==1.7.4
bcrypt==4.0.1                     # pinned <4.1: passlib 1.7.4 mis-reads newer versions (harmless warning)

# --- Reporting -----------------------------------------------------------
reportlab==4.2.5                  # compliance-report PDF rendering

# --- Config -----------------------------------------------------------
python-dotenv==1.0.1
